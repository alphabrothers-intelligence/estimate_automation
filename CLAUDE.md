# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트를 개발할 때 반드시 따라야 하는 규칙이다.
모든 규칙은 개발 시작부터 종료까지 항상 적용된다.

---

## 1. 프로젝트 개요

데이터솔루션팀의 견적서 발급 업무(최초 생성보다는 **수정과 재발급의 반복**이 실제 병목)를 자동화하는 웹 서비스.
과업종류와 총액만 입력하면 계약 법인(테스티파이 / 알파브라더스 / 블렌디드랩 / 썬데이워커 / ABBG)별 항목 카탈로그를 참조해 항목과 금액이 자동 배분되고, 법인별 양식(로고·사업자정보·도장·컬럼구조)으로 견적서 PDF가 발급된다. 본견적서 외에 비교견적서(1~3개 법인, 마크업 적용)를 함께 생성할 수 있으며, 채팅 기반 자연어 명령 또는 화면 직접 편집으로 즉시 재계산·재발급한다.

**PRD 문서:** `견적서_자동화_PRD_v0.1.md` (문서 내 표기 상태: Draft v0.4 — 8장 "미해결 질문"은 영민 팀장 컨펌 필요, 항상 최신 버전 기준으로 개발)

---

## 2. 기술 스택

**백엔드 선택 기준**: 프로젝트가 단순하면 Vercel(Next.js API Routes/서버리스 함수)로 구현한다. 네이티브 바이너리 의존성, 긴 처리시간, 헤드리스 브라우저 등 Vercel 서버리스 환경에서 배포가 어려운 복잡한 로직이 필요해지면 그때 FastAPI(별도 컨테이너 백엔드)를 도입한다.

**이 프로젝트(견적서 자동화)의 적용 결과 — FastAPI 채택.**
사유: 사용자가 제공한 5개 법인 실제 견적서가 **원본 xlsx 파일**(로고·도장 이미지가 이미 내장되어 있고, 수식으로 합계·부가세가 계산됨)이라는 걸 확인함(2026-07-07). 이 원본을 그대로 재사용하는 방식(가변 셀만 채운 뒤 PDF로 변환)이 채택되면서, 서버에 **openpyxl + LibreOffice headless**를 설치해야 함 — Vercel 서버리스 환경은 이런 무거운 네이티브 의존성을 안정적으로 올리기 어려워 컨테이너 기반 FastAPI가 필요함. LibreOffice headless는 무료·자체 호스팅이라 견적 데이터가 외부로 나가지 않고, 사용자는 브라우저로만 접속하므로 "본인 노트북에서 써야 한다"는 요구사항과도 호환됨.

| 영역 | 기술 |
|------|------|
| 프론트엔드 | Next.js (App Router) — Vercel 배포 |
| 백엔드 | FastAPI — Vercel 외 컨테이너 플랫폼(예: Render/Railway/Fly.io) 배포 |
| AI | Anthropic Claude API (`claude-sonnet-4-6`) — 항목 자동배분(7.2) 및 채팅 기반 수정 파싱(7.3)에 사용 |
| 스타일링 | Tailwind CSS |
| DB | Supabase (PostgreSQL + Storage — 법인별 마스터 xlsx 템플릿, 생성된 PDF 저장) |
| ORM | supabase-py |
| PDF 생성 | **openpyxl**(마스터 xlsx의 가변 셀만 채움) + **LibreOffice headless**(`soffice --headless --convert-to pdf`로 xlsx→PDF 변환, 수식 재계산·이미지 보존은 엔진이 처리) — 법인 원본 파일(로고·도장·문구 내장)을 그대로 재사용하고 새로 만들지 않음 |

**PDF 생성 방식 변경 이력**: 최초에는 WeasyPrint(HTML/CSS 템플릿을 직접 제작해 PDF 변환)로 설계했으나, 사용자가 제공한 5개 법인 실제 xlsx 원본을 확인한 뒤 위 방식으로 변경함(2026-07-07). 원본 파일을 그대로 쓰는 게 로고·도장·문구·컬럼구조를 재현하는 것보다 훨씬 정확하고 작업량이 적기 때문. 동시 변환 요청이 늘어 LibreOffice 프로세스 락 문제가 생기면, 같은 LibreOffice를 감싸는 전용 변환 마이크로서비스 **Gotenberg**로 교체를 검토한다(지금은 사용자 1인이라 불필요).

**인증: 현재 미도입.** 사용 인원이 1인(영민 팀장)이라 로그인 없이 개발 진행(2026-07-07 사용자 결정). 다인 사용이 필요해지면 별도 논의 후 Supabase Auth 등을 재도입한다.

위 기준으로 이미 결정된 항목(프론트엔드=Vercel, 백엔드=FastAPI 등)을 임의로 바꾸지 않는다. 판단 기준이 실제로 달라졌다고 판단되면 **즉시 중단하고 사용자에게 알린다.**

---

## 3. 폴더 구조

PRD 문서에는 별도 폴더 구조 섹션이 없어, 아래 구조를 이 프로젝트의 기준으로 삼는다.

```
project-root/
├── frontend/            # Next.js App (견적 생성 플로우, 채팅 수정 UI, 직접 편집 화면)
└── backend/             # FastAPI App
    ├── migrations/      # SQL 마이그레이션 파일 (EntityTemplate/EstimateSet/EntityQuote/QuoteVersion/ItemCatalog/QuoteTemplate)
    ├── scripts/         # migrate.py (up/down)
    ├── templates/       # 법인별 마스터 원본 xlsx/xls 파일 그대로 (로고/도장/문구 내장, 절대 재작성하지 않음)
    └── app/
        ├── routers/     # API 엔드포인트
        ├── services/    # 항목 자동배분, 채팅 수정 파싱, PDF 렌더링(openpyxl 셀 채우기 + LibreOffice 변환) 로직
        └── models/      # Pydantic 모델
```

---

## 4. 핵심 개발 규칙

### 4.1 PRD 기준 개발

- 모든 개발은 PRD를 기준으로 한다.
- 개발 중 PRD와 다르게 구현해야 하는 상황이 생기면 **즉시 중단**하고 아래를 전달한다.
  - 어떤 부분이 PRD와 다른지
  - 왜 달라질 수밖에 없는지
  - 어떤 선택지가 있는지 (최소 2가지 제안)
- 사용자가 의사결정한 후에만 계속 진행한다.

### 4.2 에러 처리

- 에러가 발생하면 **즉시 중단**하고 아래를 전달한다.
  - 어느 파일 / 어느 라인에서 발생했는지
  - 에러 메시지 원문
  - 해결 방법 제안 (최소 2가지)
- 사용자가 해결 방법을 선택한 후에만 계속 진행한다.
- 에러를 임의로 우회하거나 무시하지 않는다.

### 4.3 단계별·모듈별 개발

- 기능 전체를 한 번에 구현하지 않는다.
- 아래 순서로 모듈 단위로 개발하고, 각 모듈 완료 후 사용자에게 보고한다.

```
Phase 1. DB 마이그레이션 (EntityTemplate / EstimateSet / EntityQuote / QuoteVersion / ItemCatalog / QuoteTemplate 테이블 + 법인 5개·6장 카탈로그·셀 매핑 데이터 시딩)
Phase 2. 법인 마스터 템플릿 등록 (원본 xlsx/xls를 Supabase Storage에 업로드, QuoteTemplate 경로 갱신)
Phase 3. 견적 생성 플로우 (과업종류 선택 → 취급 법인 필터링 → 본견적 법인 선택 → 비교견적 법인 선택(1~3개) → 총액 입력)
Phase 4. Claude API 연동 — 항목 자동생성 + 금액 자동배분(7.2) + VAT 계산(7.1-7)
Phase 5. PDF 렌더링 (openpyxl로 마스터 xlsx 가변 셀 채우기 + LibreOffice headless로 PDF 변환, 내부용/고객발송용 2종)
Phase 6. 비교견적서 생성 (마크업 override, 미러링형/독립형 항목 구성)
Phase 7. 채팅 기반 수정 + 직접 편집 (수정 범위 판별: 이번 건만 vs 카탈로그 갱신 — 4.6)
Phase 8. 버전/이력 관리 및 요청 이력 조회
```

- 각 Phase 시작 전 반드시 사용자에게 "Phase N 시작합니다. 진행할까요?" 라고 확인한다.
- 각 Phase 완료 후 "Phase N 완료. 다음으로 넘어갈까요?" 라고 보고한다.

### 4.4 PRD 업데이트

- 모든 Phase가 완료된 후, PRD와 달라진 부분이 있으면 아래 형식으로 정리해서 알린다.

```
[PRD 변경 사항 보고]
- 변경 항목: (섹션명)
- 기존 내용: ...
- 변경된 내용: ...
- 변경 이유: ...
```

- 사용자가 승인하면 PRD를 새 버전으로 업데이트한다.
- 버전 형식: `v{major}.{minor}` (예: v1.4 → v1.5)

---

## 5. 코드 작성 규칙

- 환경변수는 반드시 `.env` / `.env.local`에서 읽는다. 코드에 직접 입력하지 않는다.
- 새 파일 생성 시 3장 폴더 구조를 따른다. 임의로 구조를 바꾸지 않는다.
- DB 스키마 변경이 필요하면 반드시 `migrations/` 에 SQL 파일로 추가한다.
- 커밋 메시지는 Conventional Commits 형식을 따른다: `type: 작업 내용` (제목은 한국어, type은 영어).
  - type: `feat`(기능 추가) `fix`(버그 수정) `docs`(문서) `style`(포맷) `refactor`(리팩터링) `test`(테스트) `chore`(기타 잡일) `ci` `perf` `rename` `remove`
  - 예: `feat: 과업종류 교차선택 통합 및 견적 발급 안정화`, `fix: 비교견적서 상품구성 유실 버그 수정`
  - (2026-08-11 정정: 과거 `[Phase N] 작업 내용` 형식으로 적혀 있었으나 실제 커밋 이력과 맞지 않아 실제 관례로 수정함)

---

## 6. 금지 사항

- PRD에 없는 기능을 임의로 추가하지 않는다.
- 기술 스택을 임의로 변경하지 않는다.
- 에러를 무시하거나 `try/catch`로 덮어씌우지 않는다.
- 사용자 확인 없이 다음 Phase로 넘어가지 않는다.
- `.env` 파일을 Git에 커밋하지 않는다. (`.gitignore`에 반드시 포함)