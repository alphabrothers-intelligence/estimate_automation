-- up
-- 견적서 머리글의 "업무 기간 / 제작기간" 칸 (2026-08-25 요청).
--
-- 타사 양식(위드앤코 E7 "약 60일", 테키 D10 "1.5개월 예상")에 이 칸이 있는데 우리 데이터에
-- 대응 값이 없었다. 그래서 원본에 남아 있던 **남의 견적 건 숫자**가 발급본에 그대로 찍혔다.
-- 비우면 라벨만 덩그러니 남으므로, 확정된 견적 항목의 작업일에서 초안을 뽑아 채우고
-- 실무자가 화면에서 언제든 고칠 수 있게 한다(용역명·수신자와 같은 취급).
--
-- 초안은 항목들의 **최대 작업일**이다. 과업은 대체로 병행하므로 합이 아니라 가장 긴 항목이
-- 전체 기간을 정한다 — 위드앤코 원본도 소요기간이 20/14/60/20일인데 업무 기간이 "약 60일"이다.
alter table entity_quotes
    add column if not exists duration_text text;

comment on column entity_quotes.duration_text is
    '견적서 머리글의 업무/제작 기간 표기(예: "약 60일"). 생성 시 항목 최대 작업일로 초안을 '
    '채우고 이후에는 사용자가 고친 값을 그대로 쓴다. 이 칸이 없는 양식에서는 무시된다.';

-- down
alter table entity_quotes drop column if exists duration_text;
