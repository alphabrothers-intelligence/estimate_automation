# 견적서 자동화 시스템 PRD (Draft v0.11)

**작성일**: 2026-07-07
**상태**: 초안 — "8. 미해결 질문" 항목은 영민 팀장 컨펌 필요
**표본 현황**: 고객사 2곳(미구, 단아한화식) + 5개 법인 실제 원본 파일(xlsx/xls) 전체 확보 완료. ABBG만 값이 채워지지 않은 빈 마스터(실단가 데이터 없음)
**v0.3 변경 요약**: 5개 법인 실제 원본 파일 확인 결과를 반영 — (1) 4.2 법인별 양식 관리를 "원본 파일 그대로 재사용 + 가변 셀만 채우는" 방식으로 확정, (2) 5장 데이터 모델에 `QuoteTemplate` 추가 및 `EntityTemplate` 단순화, (3) 6.2 테스티파이 마케팅·6.5 알파브라더스 전체 모듈(FGI/사용성테스트/기술성테스트/통합패키지)의 실제 항목별 단가·비중·상품구성 설명 확보·반영
**v0.4 변경 요약**: 1.4절 "5개 법인은 서로 다른 사업 도메인" 판단을 정정 — **5개 법인 모두 마케팅/광고대행/고객검증/시장검증 4개 과업종류를 공통으로 취급**함을 확인 (ABBG의 "UXUI/디자인", 썬데이워커의 "행사운영"은 실제 취급 업무가 아니라 원본 견적서 양식의 예시 내용이었음). 이에 따라 (1) "UXUI/디자인 제작" 과업종류 삭제, (2) 과업종류·법인 선택 시 필터링 제거(본견적서 제외 전체 법인이 항상 후보), (3) 법인별 실제 카탈로그(6장)는 유지하되 미확보 조합은 Phase 4에서 타 법인 카탈로그를 임시 차용하는 정책으로 변경
**v0.5 변경 요약**: 견적 생성 플로우 사용자 피드백 반영 (2026-08-08) — (1) 4.3 본견적서 필수 조건 완화: 본견적/비교견적 각각 독립 선택, 단 최소 1개 이상 선택 필수(본견적 없이 비교견적만 발급 가능) — **코드 확인 결과 이미 구현되어 있었음**(마이그레이션 020, generation_service.py), (2) 4.3 비교견적서 화면에 마크업 비율(+N%) 고지 문구 요구사항 추가 — **이 역시 이미 구현되어 있었음**, (3) 6.2 테스티파이 마케팅에 카카오 톡스토어/네이버 쇼핑 광고 대행 실데이터 반영(마이그레이션 027, alt_group 방식) — 사용자가 제공한 실 발급 샘플(우유곳간_260709) 기반, 네이버 쇼핑은 샘플 없어 카카오 톡스토어 구성을 그대로 미러링. "광고형 마케팅 대행 용역"은 마케팅이 아닌 6.3 광고대행 소속임을 재확인(이미 마이그레이션 016에 반영되어 있었음), (4) 4.3 비교견적서 항목명을 본견적서와 다른 단어로 구성하는 원칙 추가(구체 매핑 규칙은 8장 질문 11로 등록). "프로젝트 단위"(RFP 업로드 기반 견적, 스케일업팀용)는 사용자가 명시적으로 추후 개발 대상으로 지정 — 이번 버전 범위 제외(10.4 참고)
**v0.6 변경 요약**: 사용자 정정(2026-08-08) — **"광고대행"은 마케팅과 별개인 과업종류가 아니라 마케팅 안의 대안 상품**이었다(1.4). v0.4에서 4개 과업종류로 확정했던 것을 취소하고 3개(마케팅[광고대행 포함]/고객검증/시장검증)로 되돌림. 테스티파이/썬데이워커의 "광고형 마케팅 대행"과 블렌디드랩의 "Meta 광고 테스트 기획 및 운영"을 마케팅의 alt_group 대안 상품으로 편입(마이그레이션 028, 029), 6.2/6.3/부록B 재구성. 위저드가 이미 "마케팅"을 노출하고 있어 별도 UI 작업 없이 새 옵션이 그대로 선택 가능해짐(8장 질문 12 해결)
**v0.7 변경 요약**: 사용자 결정(2026-08-09) — **4.4 "직접 편집" 대상을 항목명·금액에서 단가·작업일·투입인력·비고까지 확장**. 기존엔 단가/작업일/수량을 발급 시점에 카탈로그 기준으로 다시 계산해 채웠으나(직접편집 대상 아님), 이제 화면에서 이 값들을 고치면 그 값을 그대로 저장하고 PDF/xlsx 발급에도 그대로 반영한다(카탈로그 마스터 자체는 안 바뀌며 "이번 견적만" 적용). 셋 중 하나를 고치면 나머지로 공급가액을 다시 계산해 항상 단가×작업일×투입인력=공급가액이 맞도록 프론트엔드가 재계산해서 저장한다(원본 양식의 공급가액 칸이 실제로 이 수식으로 계산되는 셀이기 때문). 이와 별개로 PDF 렌더링 버그 2건도 수정: (1) 인쇄영역 중복 정의로 내용이 페이지 구석에 작게 찍히던 문제, (2) 항목이 배정되지 않은 빈 행에 "0"이 찍히던 문제(이제 행 자체를 숨김)
**v0.8 변경 요약**: 사용자 결정(2026-08-09) — **시장검증 항목 카탈로그를 법인별 실제 카탈로그에서 전체 법인 공통 "표준 카탈로그"로 통합**. 6.5 알파브라더스의 4개 모듈(FGI/사용성 테스트/기술성 테스트/시장성 테스트, 개조식 세부항목 포함)을 기준으로 삼아 테스티파이/블렌디드랩/썬데이워커/ABBG에 그대로 복제했다(마이그레이션 030). 이 결정은 **1.4의 "법인별 실제 항목 카탈로그는 그대로 유지" 원칙을 시장검증에 한해 뒤집는 것**이라 테스티파이/썬데이워커의 표준형·Fake-door형·PMF서베이+FGI형, 블렌디드랩의 미러링형·독립형처럼 실제 발급 이력으로 검증된 데이터가 신규 발급에는 더 이상 쓰이지 않는다. 사용자가 이를 인지하고 승인했으며 "나중에 되돌릴 수도 있으니" 원래 내용을 PRD에 참고용으로 남겨달라고 요청 — 6.1에 "[v0.8 이전, 참고용]"으로 보존했고, 마이그레이션 030의 down 블록으로 DB도 완전히 원복 가능하다. 마케팅/고객검증은 이번 변경 대상이 아니며 기존처럼 법인별 실제 카탈로그를 유지한다.
**v0.9 변경 요약**: 사용자 결정(2026-08-10) — (1) **6.1/6.5 "BM 진단 및 고도화" 항목을 시장성 테스트 모듈에만 남기고 FGI/사용성 테스트/기술성 테스트에서 삭제**(마이그레이션 031) — v0.8 통합 후 4개 모듈 전부에 이 항목이 중복 등장하던 것을 정리. 알파브라더스 고유 "통합 패키지"는 성격이 달라 이번 삭제 대상에서 제외. (2) **3.2/5장 과업종류 교차 선택을 "법인당 견적서 1건"으로 병합** — 기존엔 마케팅+시장검증을 함께 선택하면 `EntityQuote`가 2건(과업종류별 1건씩) 생겨 문서도 2개로 나뉘었으나, 이제 `EntityQuote.과업종류`가 배열이 되어 한 문서에 두 과업종류 항목을 함께 담는다. 실제 물리 양식이 같은 법인(ABBG/블렌디드랩/썬데이워커)은 자연스럽게 한 시트에 들어가고, 과업종류마다 전용 시트가 다른 법인(테스티파이/알파브라더스)은 먼저 선택한 과업종류의 시트를 우선 시도하고 칸이 모자라면 세부항목→카테고리 소계→과업종류당 1줄 순으로 자동 압축한다. (3) **6.2 마케팅 "그로스해킹형"(온라인 광고/SEO 마케팅/자사몰 데이터 세팅/그로스해킹 4개 모듈) 대안상품 묶음을 해제, 개별 체크박스로 전환** — PM이 이 4개를 자유 조합으로 선택 가능해짐(0개 이상). 광고형/카카오톡스토어형/네이버쇼핑형(대행 상품 3종)은 여전히 택1 유지. 테스티파이 마케팅 원본 시트의 카테고리 칸이 정확히 4개임을 확인해 개별 선택해도 양식이 깨지지 않음을 검증. (4) **4.3 "본견적서 없이 비교견적서만" 생성하는 경우도 정식 카탈로그 세부항목을 그대로 사용** — 기존엔 이 경우 Claude가 항목 3~5개를 자유 구성하는 별도 경로(2026-07-10 결정)를 탔으나 폐기, 늘 6장 카탈로그 기반 배분을 쓴다. (5) **[신규 미해결, 8장 질문 14]** 실무자 정보로 "썬데이워커/ABBG는 시장검증 발행 이력이 없고, 실제로는 시장검증+마케팅 비교견적=블렌디드랩·디자인=ABBG·컨설팅/AI/개발=ABBG/썬데이워커"라는 1.4와 상충하는 내용을 전달받음. 근거 확인 중 알파브라더스가 "UXUI 디자인" 업무를 실제 발급한 이력 1건(이루리랩스, 2025.07.15, SW기술자 평균임금 공표표 기반 단가 산정 — 6.5에 이미 언급된 그 표와 일치)을 확인했으나, 표본이 1건뿐이라 "디자인" 등 신규 과업종류 도입은 **보류**하기로 함(추가 확인 후 재논의).
**v0.10 변경 요약** (2026-08-10) — (1) **6.5 알파브라더스 교차 선택(마케팅+시장검증) 실구현**: v0.9(2)에서 예고한 "칸이 모자라면 자동 압축" 정책을 알파브라더스 "견적서" 시트에 실제로 적용 — 기존 5행짜리 상품 블록(13~17행)을 복제해 18~22행에 두 번째 블록을 추가했다(마이그레이션 034). 이 과정에서 기존 합계·부가세 셀(E18/T18)이 새 블록과 겹쳐 상품 데이터가 합계 숫자로 덮어써지는 버그가 발견되어 합계 셀을 23행으로 이동했다(마이그레이션 035, 2026-08-10 실제 발급 재현 확인). (2) **3.2 ABBG 담당자·연락처·이메일 입력 지원**: ABBG 마스터에는 원래 이 칸들이 있었지만 `EstimateSet` 생성 시 입력받는 값이 없어 항상 비어 있었다(마이그레이션 036) — 이제 수신자명과 같은 방식으로 세트 생성 시 한 번 입력받아 전체 `EntityQuote`에 복사한다. 다른 법인 양식은 대응 칸이 없어 그냥 무시된다. **[v0.11에서 정정] 이 칸은 ABBG 전용이 아니라 알파브라더스에도 동일하게 있음이 확인됨 — 아래 v0.11 (1) 참고.** (3) **4.5 견적 목록에 삭제·상세조회 추가** — 견적 세트 목록에서 삭제 가능, 개별 세트 상세 페이지(`/estimates/[id]`)로 이력을 다시 열어볼 수 있다. (4) PDF 변환 속도 개선: 요청마다 LibreOffice 프로세스를 새로 띄우던 것을 앱 기동 시 켜 둔 리스너 프로세스 재사용으로 변경.
**v0.11 변경 요약** (2026-08-11) — (1) **4.2 알파브라더스 "구분(대)/구분(중)"·"상품구성" 컬럼을 웹 미리보기에도 노출**: PDF에는 이미 반영되던 컬럼들이 위저드 화면(직접편집 테이블)에는 안 보이던 gap을 해소 — 백엔드가 `show_category_split` 플래그와, `columns`에 `description`/`input_mm`/`tax_amount`가 있으면 `detail_column_order`에 자동 포함해 내려주고 프론트가 그대로 렌더링한다. 이 과정에서 v0.10 (2)의 "ABBG 전용" 서술이 틀렸음을 확인 — 담당자·연락처·이메일 칸은 알파브라더스 원본 양식에도 동일하게 있는데 화면 노출 조건이 ABBG만 걸려 있던 UI 버그였다(데이터·API는 이미 정상 동작 중이었음), 조건을 두 법인 모두로 확장. (2) **7.4 썬데이워커 "투입 MM"·"세액" 파생값 로직**을 실제로 추가(7.4 참고) — 정확한 근거 데이터가 없어 작업일×수량 기반 근사치/공급가액×10%로 채우고 사용자가 직접 고치는 것을 전제로 함. (3) **버그 수정 — description("상품구성") 값 유실**: 채팅 수정(`edit_service`)과 직접 편집(`estimate_service.update_line_items`) 두 경로 모두 항목을 저장할 때 `description` 필드를 통째로 날리고 있었다. 원인은 (a) 직접 편집이 받는 `LineItemIn` 모델에 애초에 `description` 필드가 없어 저장 시 조용히 드롭, (b) 채팅 수정은 Claude 응답(category/name/amount만 있음)으로 항목을 다시 만들면서 `task_type`만 기존 값에서 재부착하고 `description`은 재부착 로직이 없었음. 두 경로 모두 기존 항목에서 (카테고리, 항목명) 키로 `description`도 함께 재부착하도록 수정, 이미 유실됐던 알파브라더스 비교견적 1건은 동일 카탈로그를 쓰는 다른 견적서의 값으로 백필 복구. (4) 상품구성 칸의 세로형 개조식("1. 2. 3.") 표시가 웹 화면에서 줄바꿈 없이 가로로 뭉쳐 보이던 문제 수정 — 저장된 값의 `\n`이 무시되고 있어 `whitespace-pre-line` 적용, 헤더 라벨("상품구성")도 좌측 정렬 대신 컬럼 중앙 정렬로 조정.

---

## 1. 배경 및 문제 정의

### 1.1 배경
- 데이터솔루션팀에서 마케팅·시장검증 등의 과업 수행 시 견적서 발급이 반복적으로 발생
- 계약 주체(법인) 5곳: 알파브라더스 / 테스티파이 / 블랜디드랩 / ABBG / 썬데이워커
- 법인마다 견적서 양식(로고, 사업자정보, 도장, 항목 구성, 문구)이 다름
- 용역 계약 특성상 비교견적서(복수 법인 명의)가 요구되는 경우가 많음

### 1.2 문제
- 실제 병목은 "최초 생성"이 아니라 **수정과 재발급의 반복**
- 자잘한 수정이 있을 때마다 양식을 열어 금액 재배분 → 부가세 계산 → PDF 재생성을 매번 수작업으로 반복

### 1.3 히스토리 분석 결과

**본견적서 vs 비교견적서 실제 비교 (마크업 전체 현황)**

| 고객사 | 과업종류 | 본견적서(법인/금액) | 비교견적서(법인/금액) | 마크업 |
|---|---|---|---|---|
| 미구 | 시장검증→고객검증 | 테스티파이 19,800,000 | 블렌디드랩 20,900,000 | +5.6% |
| 미구 | 마케팅 | 테스티파이 19,800,000 | 블렌디드랩 22,000,000 | **+11.1%** |
| 단아한화식 | 시장검증→고객검증(PMF) | 테스티파이 8,052,000 | 블렌디드랩 11,000,000 | +36.6% |
| 단아한화식 | 시장검증→광고대행(Meta) | 테스티파이 8,052,000 | 블렌디드랩 9,900,000 | +22.9% |

⚠️ **담당자 인터뷰 결과(마크업 10~15% 룰)와의 괴리**: 실측 마크업은 +5.6%~+36.6%로 편차가 큼. 미구·마케팅 건만 룰과 일치. 이 룰이 최근에 도입된 정책이라 과거 건들은 예외인지, 혹은 룰이 "가이드라인"일 뿐 실제로는 재량 범위가 넓은지 확인 필요 (8장 질문 참고)

**발견된 패턴 (전체)**

1. **비교견적서 마크업 비율은 고정값이 아니며, 담당자 인터뷰 기준 "최소 10%, 팀장 피드백 시 최대 15%"가 가이드라인** — 다만 실제 과거 데이터는 이 범위를 벗어나는 경우가 더 많음 (위 표 참고)
2. **항목 구성 방식이 2가지 존재**
   - 미러링형: 본견적서와 동일한 대분류명 사용, 세부항목은 압축, 카테고리별 금액은 불균등 재배분 (미구 사례)
   - 독립형: 완전히 다른 서비스명/구성 사용 (단아한화식 PMF Survey 사례)
3. **동일 법인·동일 고객 내 재발급 시에도 항목명이 자유롭게 변경됨**, 총액은 고정한 채 항목만 재구성하는 패턴 확인. 항목명 중복 실수 사례도 발견
4. **비교견적서의 "과업종류(카테고리)" 자체가 통째로 바뀔 수 있음** — 단아한화식 건에서 블렌디드랩이 "고객검증"(1/30, 11M) → "광고대행"(2/2, 9.9M)으로 카테고리와 금액을 모두 재구성해 재발급
5. **내부 검토용**(기준단가·실단가·절삭액 노출) **과 고객 발송용**(클린 버전) **두 종류가 같은 데이터에서 파생**되어 나감
6. 문서상 "작성일" 필드가 실제 수정일과 다르게 남아있는 사례 존재 (재발급 시 갱신 안 됨)
7. **비교견적서는 카테고리별로 균등하게 상향되지 않음** — 일부 카테고리는 감액, 다른 카테고리는 증액되며 총액만 목표치에 맞춰짐
8. **비교견적서는 세부항목을 압축해서 제시하는 경향** — 세부 항목(4~5줄)이 괄호 안에 나열되고 1줄로 통합됨
9. **"목표 총액에 딱 맞추기 위한 소액 조정" 메커니즘이 여러 법인에 공통 존재** — 테스티파이는 "절삭" 컬럼, 썬데이워커는 "DC(할인)" 컬럼으로 구현 방식은 다르지만 목적은 동일 (반올림된 단가 합계를 목표 총액에 맞추기 위한 보정)

### 1.4 [v0.6 정정] 5개 법인은 3개 과업종류(마케팅[광고대행 포함]/고객검증/시장검증) 모두에 공통으로 사용 가능

~~이전 버전(v0.1~v0.3)에서는 법인별 자체 템플릿/샘플을 근거로 "5개 법인은 서로 다른 사업 도메인이라 과업종류별로 취급 법인이 다르다"고 판단했으나, 이는 잘못된 추론이었다.~~ 확보된 각 법인 견적서 샘플은 **양식(로고·도장·컬럼구조) 예시일 뿐**이며, 실제로는 **5개 법인 모두 마케팅/고객검증/시장검증 3개 과업종류를 전부 취급**한다 (2026-07-07 팀장 확인). 즉 ABBG 샘플에 있던 "UXUI/디자인 제작"이나 썬데이워커 샘플에 있던 "행사운영"은 실제 취급 업무가 아니라 원본 파일의 예시 내용일 뿐이다.

**[v0.9, 재검토 필요 — 8장 질문 14]** 위 2026-07-07 판단과 상충하는 실무자 정보(썬데이워커·ABBG는 시장검증 미취급, 디자인/컨설팅/AI/개발이 실제 업무)를 전달받았다. 알파브라더스의 실제 "UXUI 디자인" 발급 이력 1건은 확인했으나 신규 과업종류 도입은 표본 부족으로 보류했다 — 이 표의 "실제 취급 과업" 열은 아직 갱신하지 않은 상태이며, 8장 질문 14가 해결되기 전까지는 기존 판단(공통 취급) 그대로 개발을 진행한다.

**[v0.6 추가 정정, 2026-08-08]** v0.4에서는 "광고대행"을 마케팅과 별개인 4번째 과업종류로 두었으나, 사용자 확인 결과 **광고대행은 별도 과업종류가 아니라 마케팅 안의 한 상품 유형**이다("카카오 톡스토어 광고 대행"처럼 마케팅의 다른 대안 상품들과 동급). 6.2/6.3을 이에 맞춰 재구성했고(`028_merge_ad_agency_into_marketing.sql`), 데이터 모델상으로도 테스티파이/썬데이워커의 "광고형 마케팅 대행"이 마케팅 마스터 파일(`templates/testify.xlsx` 시트 '마케팅')의 블록 구조와 정확히 일치해 이 정정이 뒷받침됨을 확인함.

| 법인 | 견적서 양식 출처 | 실제 취급 과업 |
|---|---|---|
| 테스티파이 | 미구/단아한화식 히스토리 (실제 발급 이력) | 마케팅/고객검증/시장검증 (공통) |
| 알파브라더스 | 자체 템플릿(xlsx) — 시장검증 4개 세부모듈(FGI/사용성테스트/기술성테스트/시장성테스트) 실제 이력 보유 | 마케팅/고객검증/시장검증 (공통) |
| 블렌디드랩 | 미구/단아한화식 히스토리 (실제 발급 이력) | 마케팅/고객검증/시장검증 (공통) |
| 썬데이워커 | 자체 템플릿(xlsx, 원래 행사운영 견적 샘플 — 양식만 재사용) | 마케팅/고객검증/시장검증 (공통) |
| ABBG | 자체 템플릿(xlsx, 원래 UXUI/디자인 견적 샘플 — 양식만 재사용) | 마케팅/고객검증/시장검증 (공통) |

**정책**: 과업종류·본견적서 법인·비교견적서 법인(본견적서 제외 나머지 전부) 선택에 필터링을 두지 않는다. **[v0.8 예외]** 마케팅·고객검증은 **법인별 실제 항목 카탈로그(6장)를 그대로 유지**한다 — 법인마다 실제 확보된 항목 구성·단가가 다르므로 이를 통합하지 않는다. 다만 **시장검증만은 v0.8에서 예외적으로 통합**했다 — 알파브라더스의 4개 모듈 표준 카탈로그(6.1/6.5 참고)를 5개 법인 공통으로 쓴다(사용자 결정, 되돌릴 경우 마이그레이션 030 down 참고). (법인×과업종류) 조합 중 아직 실제 카탈로그가 없는 경우(예: ABBG+고객검증), 해당 조합의 항목 생성 시 다른 법인의 카탈로그를 임시로 빌려쓰는 방식을 Phase 4(항목 자동생성)에서 설계한다.

---

## 2. 목표

1. 총액 + 과업 종류만 선택하면 항목이 자동 생성되고, 총액에 맞춰 금액이 자동 배분(부가세 반영)된다.
2. 계약 주체(법인)별 양식을 선택해 각 회사 명의로 견적서를 발급하고, 비교견적서(복수 법인)를 한 번에 뽑는다. → 추후 수정 요청이 용이하도록 내역이 저장되어야 함
3. 수정 요청이 오면 채팅으로 지시해 즉시 반영하고 PDF로 재발급한다. **(핵심 가치)**

---

## 3. 사용자 및 시나리오

### 3.1 주 사용자
- 사업 PM: 과업 범위·금액을 정하고 견적 초안을 요청 → 주로 영민 팀장

### 3.2 핵심 시나리오

**표준 생성 흐름 (v0.4 — 법인 선택이 항목/예산 구성보다 먼저, 과업종류·법인 선택에 필터링 없음)**

1. **과업종류 선택**: 마케팅/광고대행/고객검증/시장검증 중 선택 (5개 법인 모두 4개 과업종류를 공통으로 취급 — 1.4 참고). **[v0.9]** 법인 하나에 과업종류를 여러 개(예: 마케팅+시장검증) 교차 선택할 수 있으며, 이 경우 그 법인의 견적서는 두 과업종류 항목을 합쳐 **한 문서**로 발급된다(4.3 참고, 이전엔 과업종류별로 문서가 나뉘었음)
2. **본견적서 법인 선택**: 5개 법인 중 기준값이 되는 법인 선택 (예: 테스티파이)
3. **비교견적서 법인 선택 (1~3개, 복수선택)**: 본견적서로 선택한 법인을 제외한 나머지 전체(4개) 중 1~3개 선택 (예: 블렌디드랩, ABBG)
4. **총액 입력 → 항목 자동생성 + 예산 배분**: 총액(VAT포함 여부) 입력 → 선택된 법인 각각에 대해 항목 자동생성 및 금액 배분 (본견적서는 입력 총액 그대로, 비교견적서는 마크업 적용된 금액으로)
5. **생성 완료**: 선택된 모든 법인의 PDF를 한 세트로 발급

**기존 세부 시나리오** (위 표준 흐름 내에서 발생하는 개별 이벤트)

- **채팅 수정**: "3번 항목 200만원 낮추고 총액은 유지해줘" → 항목·부가세·합계 자동 재계산 → 즉시 재발급
- **직접 편집**: 채팅이 아니라 화면에서 항목/금액을 직접 수정하고 저장
- **주체 변경**: "ABBG 명의로 다시 뽑아줘" → 금액·항목은 그대로, 양식만 교체해 재발급

---

## 4. 기능 요구사항

### 4.1 항목 자동생성 + 금액 자동배분
- 입력: 총액, VAT 포함/별도 여부, 과업 종류, (법인 — 카탈로그 결정에 사용)
- 과업종류 × 법인 조합별 "항목 카탈로그"를 참조하여 항목 자동 생성 (전체 카탈로그는 6장 참고)
- 총액은 카탈로그 내 항목별 **과거 비중(%)을 참고해 비례 배분** → 상세 로직은 7장
- 부가세 10% 계산, 반올림 후 남는 차액은 마지막 항목 또는 별도 조정 필드("절삭"/"DC")에서 흡수

### 4.2 법인별 양식 관리

**[v0.3 변경] 실제 원본 파일 기반으로 구현 방식 확정.** 사용자가 제공한 5개 법인 실제 견적서 원본(xlsx/xls)을 확인한 결과, 로고·도장 이미지가 이미 파일 안에 내장되어 있고 합계·부가세 계산도 셀 수식으로 되어 있음을 확인. 따라서 양식을 새로 만들지 않고 **원본 파일을 그대로 재사용**하며, 매 견적마다 가변 셀(수신자·견적일자·항목별 수량/단가/작업일 등)만 채워 넣는 방식으로 구현한다.
- 5개 법인 마스터 원본 파일 그대로 보관 (`backend/templates/` — 이후 Supabase Storage로 이관)
- 법인 선택 + 과업종류(+모듈) 선택 시 해당 원본 파일의 지정된 시트를 열어 가변 셀만 채운 뒤 PDF로 변환 (7장·CLAUDE.md 2장 참고)
- **법인마다 견적서 컬럼 구조 자체가 다름** (원본 파일 확인 결과 확정):
  - 테스티파이: 항목·단가·작업일·수량·공급가액·비고 (카테고리별 소계 + 부가세 10% 행 + 총합계)
  - 블렌디드랩: 품명·작업일·수량·단가·금액(부가세 별도) (소계 없이 합계금액(부가세포함) 1줄)
  - 알파브라더스: 구분(대)·구분(중)·상품명·상품구성·작업일·수량·단가·공급가액
  - 썬데이워커: 품명·투입MM·수량·단가·공급가액·세액 (세액을 항목별로 별도 계산, DC 조정 필드 있음)
  - ABBG: 상품명·상세내용·소요일·작업수량·단가·공급가액
- 법인 1곳 안에서도 과업종류/모듈에 따라 시트·셀 좌표가 다른 경우가 있음 (예: 알파브라더스는 한 파일에 FGI/사용성테스트/기술성테스트/시장성테스트/통합패키지 5개 시트) → 데이터 모델에 `QuoteTemplate` 추가로 대응 (5장 참고)

**고정정보 vs 가변정보 구분** (설계상 반드시 분리 저장)

| 구분 | 내용 | 저장 방식 |
|---|---|---|
| 법인 고정정보 (상호명, 사업자등록번호, 대표자, 사업장주소, 이메일, 연락처, 업태/종목, 도장 이미지, 로고, 고정 문구) | 원본 파일 자체 안에 이미 존재 | **DB에 별도로 중복 저장하지 않음.** 견적 생성 로직이 이 값들이 있는 셀은 절대 건드리지 않음 |
| 건별 가변정보 | 수신자(고객사명), 견적일자, 용역명, 항목, 금액, 부가세, 합계 | `QuoteTemplate.cell_map`이 가리키는 셀 좌표에, 매 견적 건 생성 시 새로 채워짐 |

→ 법인을 선택하면 원본 파일은 그대로 열어 고정정보는 전혀 손대지 않고, `cell_map`이 지정한 가변 셀만 새로 채우는 구조.

### 4.3 비교견적서 생성
- **[v0.5 변경] 본견적서/비교견적서 선택은 각각 독립적** — 5개 법인 중 본견적서용 법인을 선택하거나, 비교견적서용 법인(1~3개)을 선택하거나, 둘 다 선택 가능. 단 **최소 1개 이상(본견적 또는 비교견적)은 반드시 선택**해야 발급 가능(본견적 없이 비교견적서만 발급하는 흐름 지원)
- **[v0.9 변경] 본견적서 없이 비교견적서만 발급하는 경우에도 6장 정식 카탈로그의 세부항목을 그대로 사용한다** — 이전(2026-07-10 결정)에는 이 경우 정식 카탈로그 대신 Claude가 항목 3~5개를 자유롭게 구성하는 별도 경로를 탔으나, "선택한 과업종류의 세부항목이 그대로 들어가야 한다"는 요청으로 폐기하고 본견적서와 동일한 배분 로직으로 통일했다
- **[v0.9 신규] 과업종류 교차 선택 시 한 문서로 병합** — 법인 하나에 마케팅+시장검증처럼 과업종류를 여러 개 선택하면, 그 법인의 EntityQuote는 여전히 1건이고 두 과업종류의 카탈로그·배분 결과를 합쳐 하나의 견적서 문서로 발급한다(5장 데이터 모델 참고). 물리 양식은 실제 원본 파일 구조를 따르므로, 두 과업종류가 원래 같은 시트를 쓰는 법인(ABBG/블렌디드랩/썬데이워커)은 자연스럽게 한 장에 들어가고, 과업종류마다 전용 시트가 다른 법인(테스티파이/알파브라더스)은 먼저 선택한 과업종류의 시트를 우선 시도하며 칸이 모자라면 세부항목 → 카테고리 소계 1줄 → 과업종류당 1줄 순으로 자동 압축한다
- **비교견적서는 1~3개 법인 복수 선택 가능** (본견적서가 있으면 그 법인 제외 나머지 중에서, 2개 법인 단순 비교뿐 아니라 3파전 비교견적도 지원)
- 비교견적서 마크업: **기본 +10%**, 팀장 피드백에 따라 **+15%까지 조정 가능**. 단, 히스토리상 이 범위를 벗어나는 실제 사례가 다수이므로 **PM이 채팅 또는 직접 편집으로 자유롭게 override 가능**해야 함 (고정 로직으로 강제 금지)
- **[v0.5 신규]** 비교견적서 생성 화면에는 적용된 마크업 비율을 간략히 고지하는 문구를 표시 (예: "본견적 대비 +10% 적용됨")
- 항목 구성 모드 선택 가능: 미러링형 / 독립형
- 미러링형: 카테고리 간 자유 재배분(일부 감액 + 일부 증액) 후 총액만 목표치에 맞추는 방식, 세부 항목은 괄호 안에 나열 후 1줄로 압축하는 옵션
- **비교견적서의 과업종류(카테고리) 자체를 본견적서와 다르게 설정하는 것도 지원** (예: 본=시장검증, 비교=광고대행)
- **[v0.5 신규]** 비교견적서의 항목명은 본견적서와 맥락은 같되 다른 단어로 구성하는 것을 원칙으로 함 (동일 문구로 두 견적서를 나란히 발급 시 고객사가 비교견적의 진위를 의심할 수 있음). 구체적인 단어 매핑 규칙은 미확정 — 8장 질문 11 참고

### 4.4 채팅 기반 수정 + 직접 편집 (핵심 가치)
- 자연어 명령 파싱 → 대상 항목 / 변경 액션(증액·감액) / 제약조건(총액 유지 등) 추출
- 실제 발견된 수정 유형 4가지 지원
  1. 항목명 변경 (금액 불변)
  2. 항목 통합/분리 (총액 불변, 항목 구성만 재편)
  3. 특정 항목 금액 조정 + 총액 유지 → 나머지 항목 자동 재배분
  4. 법인 교체 (금액·항목 불변, 양식만 교체)
- **신규**: 채팅 없이 화면에서 항목/금액을 직접 클릭하여 수정 후 저장하는 기능 (담당자가 사소한 조정은 채팅보다 직접 수정을 선호할 수 있음)
- **[v0.7 확장]** 직접 편집 대상을 항목명·금액에서 **단가·작업일·투입인력(수량)·비고**까지 확장. 단가/작업일/투입인력 중 하나를 고치면 나머지 값을 유지한 채 공급가액을 다시 계산하고(반대로 공급가액을 고치면 단가를 역산), 그 값을 그대로 저장해 PDF/xlsx 발급에도 반영한다 — 발급 시점에 카탈로그 기준으로 재계산하지 않는다. 이번 견적에만 적용되며 카탈로그(6장) 자체는 바뀌지 않는다(4.6 "① 이번 견적만 수정"에 해당)
- 재계산 후 새 버전 생성 (기존 버전은 보존), 재발급 시 작성일 자동 갱신

### 4.6 수정 범위의 2단계 구분 (중요 — 카탈로그 vs 개별 견적, 둘 다 채팅으로 처리)

채팅 수정 기능은 개별 견적 수정뿐 아니라 **카탈로그(마스터 템플릿) 수정까지 모두 담당**해야 함. 카탈로그 수정을 별도 화면/경로로 분리하면, 그 자체가 이 프로젝트가 없애려는 "수작업"이 되어버리므로 지양. 다만 사용자의 발화가 "이번 건만"을 의미하는지 "앞으로도 계속"을 의미하는지는 **채팅 파서가 의도를 구분**해야 함

| 범위 | 대상 | 영향 범위 | 발화 예시 |
|---|---|---|---|
| **① 이번 견적만 수정** (기본값) | `EntityQuote` 인스턴스 | 해당 사업 건 1개에만 적용 | "3번 항목 200만 낮춰줘" |
| **② 카탈로그(마스터 템플릿) 갱신** | `ItemCatalog` 마스터 데이터 | (법인×과업종류) 조합의 **향후 모든 신규 견적 생성**에 반영 | "이 항목명은 앞으로도 이걸로 써줘", "이 비중을 기본값으로 저장해줘" |

- 파서가 "이번 건/앞으로도" 같은 명시적 신호를 못 찾을 경우 → 기본값은 ①(이번 견적만). 발화가 애매하면 "이번 견적에만 반영할까요, 카탈로그 기본값도 바꿀까요?"로 1회 확인 후 진행
- 실무상 과업종류·항목 구성 자체가 바뀌는 일은 드물다는 점(담당자 언급)을 감안해, ②는 빈도는 낮지만 **반드시 지원되어야 하는 기능**으로 설계 — "거의 안 쓰니까 나중에"로 미루지 않음
- ② 실행 시에도 이전 카탈로그 버전은 보존(버전 이력) — 마스터 데이터도 QuoteVersion과 마찬가지로 이력 관리 필요
- 권한 문제도 함께 고려 필요: 모든 PM이 채팅으로 카탈로그를 바꿀 수 있는지, 특정 담당자만 가능한지는 8장 질문 참고

### 4.5 버전/이력 관리
- 사업 건(세트) 단위로 모든 법인 견적서 + 버전 이력 저장
- 수정 요청 원문(채팅 로그) 및 직접 편집 이력 모두 보존 → 추후 "왜 이렇게 바뀌었는지" 재조회 가능

---

## 5. 데이터 모델 (v0.3)

```
EntityTemplate (법인 참조 키 — 5개 법인당 1건)
 └─ 법인명

QuoteTemplate (법인×과업종류×모듈별 원본 파일 경로 + 셀 좌표 매핑 — [v0.3 추가])
 ├─ 법인 (→ EntityTemplate 참조)
 ├─ 과업종류, 모듈명 (모듈이 없으면 공란 — 예: 블렌디드랩/썬데이워커/ABBG는 과업종류당 1건)
 ├─ 원본_파일_경로 (마스터 xlsx/xls, 로고·도장·사업자정보·고정문구가 이미 내장되어 있어 절대 건드리지 않음)
 ├─ 시트명 (한 파일 안에 모듈별로 시트가 다른 경우 대응 — 예: 알파브라더스 5개 시트)
 └─ 셀_매핑 (수신자/견적일자 등 헤더 가변 셀 좌표, 항목 테이블 시작행·컬럼 좌표, 합계/부가세 셀 좌표)

EstimateSet (견적 세트 = 사업 건 1개)
 ├─ 사업명, 총액, VAT포함여부, 과업종류, 생성일
 ├─ 본견적서_법인 (PM이 지정, 건마다 다름)
 │
 ├─ EntityQuote (법인별 견적서, 세트당 "법인 선택" 개수만큼 — [v0.9] 과업종류를 교차 선택해도 법인당 1건)
 │   ├─ 법인 (→ EntityTemplate 참조, 고정정보는 여기서 가져다 씀)
 │   ├─ is_primary (본견적서 여부 — 세트 내 정확히 1개만 true)
 │   ├─ 과업종류들 (배열, 본견적서와 다를 수 있음 — 발견4 근거) — **[v0.9]** 마케팅+시장검증처럼 교차
 │   │   선택하면 이 배열에 여러 값이 들어가고, 두 과업종류의 항목이 한 문서(한 EntityQuote)에 함께
 │   │   담긴다. 표시용으로는 "마케팅+시장검증"처럼 이어붙인 라벨도 함께 저장
 │   ├─ 수신자(고객사명), 견적일자, 용역명
 │   ├─ 총액, LineItems[](항목/단가/수량/부가세/상품구성설명/**소속 과업종류** — 교차 선택 시 어느
 │   │   과업종류 카탈로그에서 왔는지 항목마다 기록해, 발급 시 그 과업종류의 카탈로그로 단가를 계산)
 │   └─ 현재버전_id
 │
 ├─ QuoteVersion (수정 이력)
 │   ├─ version_no, 수정요청_원문(채팅로그 또는 "직접편집" 표기)
 │   ├─ 변경내역(diff), 수정시각/수정자
 │   └─ PDF_경로 (내부용/고객발송용 2종)
 │
 └─ ItemCatalog (법인×과업종류별 표준 항목 카탈로그 — 마스터 데이터, 세트와 별개로 관리)
     ├─ 법인, 과업종류, 모듈명 (예: "설문형 시장검증"은 옵션 모듈)
     ├─ 항목명, 표준_상품구성_설명, 과거_비중(%)
     ├─ 필수여부 (옵션 모듈 판단용)
     └─ 공유출처_법인 (선택) — 예: 썬데이워커의 "시장검증" 카탈로그는 테스티파이 카탈로그를 그대로 참조/복제. 항목 내용은 공유하되 렌더링 양식(컬럼구조)은 법인 고유 템플릿 사용
```

---

## 6. 항목 카탈로그 (과업종류 × 법인, 확인된 전체 목록)

### 6.1 시장검증 — 전체 법인 공통 표준 카탈로그 [v0.8]

**[v0.8, 2026-08-09 사용자 결정]** 시장검증은 더 이상 법인별로 다른 카탈로그를 쓰지 않는다. 알파브라더스가 보유한 4개 모듈(FGI/사용성 테스트/기술성 테스트/시장성 테스트, 개조식 세부항목 포함 — 상세는 6.5 참고)을 5개 법인 공통 표준으로 삼아 테스티파이/블렌디드랩/썬데이워커/ABBG의 item_catalogs에 그대로 복제했다(마이그레이션 `030_unify_market_validation_catalog.sql`, `shared_source_entity_id`로 출처 표시). PM은 법인과 무관하게 이 4개 모듈 중 하나를 alt_group 대안으로 선택한다. 알파브라더스의 "통합 패키지"(4개 모듈 번들)는 알파브라더스 고유 상품이라 표준 카탈로그에서 제외했고, 알파브라더스 자신의 항목은 그대로 유지된다.

**[v0.9 정정]** 위 통합 결과 "BM 진단 및 고도화"가 4개 모듈 전부에 첫 세부항목으로 중복 등장하는 문제가 발견되어, **시장성 테스트에만 남기고 FGI/사용성 테스트/기술성 테스트에서는 삭제**했다(마이그레이션 `031_remove_duplicate_bm_diagnosis_item.sql`, 5개 법인 전체 적용). 알파브라더스 고유 "통합 패키지"의 BM 진단 항목(번들 구성요소 중 하나)은 성격이 달라 이번 삭제 대상에서 제외했다.

이 결정은 1.4의 "법인별 실제 항목 카탈로그는 그대로 유지" 원칙을 시장검증에 한해 뒤집는 것이라, 아래 "[v0.8 이전, 참고용]"에 있는 법인별 실제 발급 이력 기반 카탈로그는 더 이상 신규 발급에 쓰이지 않는다. **되돌릴 가능성을 위해 원래 내용을 참고용으로 남겨두었으며**, 마이그레이션 030의 down 블록으로 DB도 완전히 원복 가능하다.

#### [v0.8 이전, 참고용] 테스티파이 자체 시장검증 카탈로그

**모듈 A. 시장성 테스트** (필수)
| 항목 | 참고 비중(미구 기준) |
|---|---|
| BM 진단 및 고도화 | 5.6% |
| MVP 광고안 기획(및 제작) | 44.4% |
| 정량 데이터 시계열 분석 | 22.2% |
| 정량 데이터 클러스터 분석 | 22.2% |
| 결과 리포트 제공 | 5.6% |

**모듈 B. 설문형 시장검증** (옵션 — 미구 건은 포함, 단아한화식 Fake-door 건은 미포함)
| 항목 | 참고 비중(미구 기준) |
|---|---|
| 설문 설계 | 5.6% |
| 설문응답자 모집 및 운영 *리워드 포함 | 44.4% |
| 설문 데이터 가공 및 시각화 | 38.9% |
| 결과 리포트 제공 | 11.1% |

**단아한화식(Fake-door test형, 모듈 A 변형) — 실제 발급 샘플 확보 (2026-01-30, 총 합계 8,052,000원 VAT포함)**

모듈 A의 3번째 항목이 "정량데이터분석" 대신 "Meta 광고 세팅 및 운영"으로 대체된 변형. 별도 모듈 `시장성 테스트 (Fake-door형)`로 카탈로그에 저장(6.1 표준형과는 독립적인 모듈 — PM이 발급 시 표준형/Fake-door형 중 선택):

| 항목 | 단가 | 비중 |
|---|---|---|
| BM 진단 및 고도화 | 500,000 | 6.83% |
| MVP 광고안 기획 | 2,500,000 | 34.15% |
| Meta 광고 세팅 및 운영 | 3,620,000 | 49.45% |
| 결과 리포트 제공 | 700,000 | 9.56% |
| 공급가액 합계 | 7,320,000 | 100% |

**단아한화식(Survey,Interview형 — PMF Survey + FGI, 모듈 A/B와 상호 배타)** — 실제 발급 샘플(총 합계 13,200,000원 VAT포함). "PMF Survey"(설문 기획 및 운영/설문 결과 및 분석 리포트 제공, 각 50%)와 "FGI (심층그룹인터뷰)"(인터뷰 기획 및 운영/인터뷰 결과 및 분석 리포트 제공, 각 50%) 두 모듈로만 구성 — 시장성 테스트 없이 발급된 완전히 다른 견적.

썬데이워커는 이 테스티파이 카탈로그(표준형/Fake-door형/서베이인터뷰형) 전체를 그대로 미러링해서 썼다(6.6 참고).

### 6.2 마케팅 — 테스티파이 또는 블렌디드랩

**[v0.6 정정]** "마케팅" 과업종류는 상호 배타적인 여러 대안 상품(alt_group) 중 PM이 하나를 고르는 구조다 — 6.5 알파브라더스 시장검증의 alt_group 방식과 동일. 아래 표기의 "대분류"는 실제로는 **상품명**이고, 각 상품은 서로 다른 실제 고객사에게 발급된 완전히 다른 구성이다. "광고대행"이라는 별도 과업종류는 없다(1.4 v0.6 참고) — 광고형 상품들도 전부 이 6.2에 속한다. **[v0.9 정정]** 다만 아래 ①은 더 이상 택1 대안 상품이 아니라 개별 체크박스로 자유 조합 가능하다 — 상세는 아래 참고.

**테스티파이/썬데이워커 — 대행 상품 3종(택1) + 개별 모듈 4종(자유 조합)** (알파브라더스/ABBG는 마케팅 실카탈로그가 없어 폴백 차용, 부록 B 참고)

**[v0.9 변경]** 아래 ①(온라인 광고/SEO 마케팅/자사몰 데이터 세팅/그로스해킹)은 원래 "그로스해킹형"이라는 하나의 대안 상품(택1)으로 묶여 있었으나, PM이 "온라인 광고만 필요하고 SEO는 필요없는" 등 세부 조합을 고를 수 없다는 문제가 있어 **개별 체크박스(0개 이상 자유 선택)로 분리**했다(마이그레이션 033). 테스티파이 마케팅 원본 시트가 이 4개 모듈용 카테고리 칸을 정확히 4개 갖고 있어, 몇 개를 고르든 원본 양식 그대로 정확한 칸에 들어간다. ②③④(광고형/카카오톡스토어형/네이버쇼핑형, 대행 상품 3종)는 실제 발급 샘플로 상호 배타 관계가 확인된 별도 상품이라 **택1 그대로 유지**한다.

**① 온라인 광고 / SEO 마케팅 / 자사몰 데이터 세팅 / 그로스해킹** (개별 체크박스, 자유 조합) — 실제 발급 샘플(미구, 260211/260324, 공급가액 합계 18,000,000원)

| 대분류 | 세부항목 | 비중 |
|---|---|---|
| 온라인 광고 | 마케팅 전략안 | 2.78% |
| | USP 설정 및 제안 | 2.78% |
| | 이미지형 광고 소재 제작 | 22.22% |
| | Meta 광고 세팅 및 운영 | 16.67% |
| SEO 마케팅 | SEO 키워드 분석 및 제안 | 2.78% |
| | 테크니컬 SEO 최적화 | 8.33% |
| | SEO 성능 측정 및 보고 | 1.11% |
| 자사몰 데이터 세팅 | 자사몰 데이터 연결 | 2.78% |
| | GA4·GTM 데이터 연결 | 2.78% |
| | 대시보드 구축 및 데이터 시각화 | 11.11% |
| 그로스해킹 | 주간 단위 데이터 분석 | 13.33% |
| | 주간 단위 액션플랜 수행 | 13.33% |

**② 광고형** ("광고형 마케팅 대행 용역") — 실제 발급 샘플(미구 마케팅 대행 견적서_260211.pdf, 공급가액 합계 15,000,000원). **[v0.6 정정]** v0.5에서 "6.3 광고대행 과업종류 소속"으로 잘못 분류했던 것을 여기로 재편입 — 테스티파이 마케팅 마스터 파일(`templates/testify.xlsx` 시트 '마케팅')의 4개 블록 중 첫 블록과 정확히 일치함을 확인

| 항목 | 단가 | 작업일·수량 | 공급가액 | 비중 |
|---|---|---|---|---|
| 광고 대시보드 구축 | 2,000,000 | 1개월 × 1명 | 2,000,000 | 13.33% |
| 광고 집행 및 운영 | 5,000,000 | 2개월 × 1명 | 5,000,000 | 33.33% |
| 데이터 분석 | 5,000,000 | 2개월 × 1명 | 5,000,000 | 33.33% |
| 결과 리포팅 및 고도화 설계 | 3,000,000 | 1개월 × 1명 | 3,000,000 | 20.00% |

**③ 카카오톡스토어형** ("카카오 톡스토어 광고 대행") — 실제 발급 샘플(우유곳간 마케팅 견적서_260709.xlsx, 공급가액 합계 3,500,000원)

| 항목 | 비중 |
|---|---|
| 전략 기획 | 28.57% |
| 광고 운영 | 57.14% |
| 결과 보고 | 14.29% |

**④ 네이버쇼핑형** ("네이버 쇼핑 광고 대행") — 실 샘플 없음. 사용자 지시로 카카오톡스토어형과 동일한 항목·비중을 그대로 반영(2026-08-08). 항목 구성은 ③과 동일

- 썬데이워커는 6.6 원칙대로 모두 테스티파이 카탈로그를 그대로 미러링
- DB 반영: `backend/migrations/027_add_marketing_alt_products.sql`(③④ 추가), `028_merge_ad_agency_into_marketing.sql`(②를 광고대행에서 이관), `029_fix_marketing_alt_group_defaults.sql`(alt_group 기본값 중복 버그 수정), `033_split_growth_hacking_marketing_modules.sql`(①을 택1 대안상품에서 개별 체크박스로 분리)

**블렌디드랩 — 대안 상품 2종**

**① 마케팅기본형** (미러링형 — 테스티파이 4개 대분류명을 그대로 쓰되 세부항목 압축, 기본값)
- 온라인 마케팅(=온라인광고) / 퍼포먼스 마케팅(=SEO마케팅) / 자사몰 데이터 세팅 / 그로스해킹
- 재발급 시 "퍼포먼스 마케팅"→"SEO 마케팅"으로, "자사몰 데이터 세팅"+"그로스해킹"→"SNS 마케팅" 1개로 통합되는 등 자유 변형 사례 존재

**② 단일광고형** ("Meta 광고 테스트 기획 및 운영") — 표본 1건뿐(단아한화식): 1줄로 총액 전체 구성. **[v0.6 정정]** v0.5 이전엔 "6.3 광고대행"으로 별도 취급했으나 마케팅의 대안 상품으로 재편입(블렌디드랩은 애초 두 과업종류가 동일한 물리 템플릿·시트를 공유하고 있었음)

- 표기 규칙: 알파브라더스는 대/중분류에 대분류명을 그대로 쓰고 중분류란엔 "마케팅" 고정, 상품명에 위 상품명, 상품구성에 세부항목을 세로형 개조식(1. 2. 3.)으로 나열. 나머지 법인(선데이워커/ABBG)은 자체 실 발급 샘플이 없어 표준 표기 규칙(볼드체 중분류 + 세부항목 나열)을 따름

### 6.3 [v0.6 폐기 — 내용 전체 6.2로 통합] 광고대행

이 절에서 다루던 "광고대행"은 별도 과업종류가 아니라 마케팅 안의 대안 상품이었음이 확인되어(1.4 v0.6 참고) 6.2로 옮겼다. 절 번호는 8장·부록 B 등 다른 곳에서 참조 중이라 유지하되, 내용은 비워둔다.

### 6.4 [v0.8 이전, 참고용] 고객검증(=시장검증) — 블렌디드랩

**[v0.8]** 6.1과 동일한 이유로 더 이상 신규 발급에 쓰이지 않는다 — 블렌디드랩의 시장검증도 이제 6.1/6.5의 알파브라더스 표준 카탈로그(4개 모듈)를 그대로 쓴다. 아래는 되돌릴 경우를 위한 참고용 원본 내용(마이그레이션 030 down으로 복원 가능).

- **미러링형** (미구): "시장성 테스트"(BM진단/MVP제작/데이터분석/결과리포트) + "설문형 시장검증"(설문설계/모집운영/설문가공/결과리포트) — 테스티파이 대분류명 그대로 사용, 세부항목 압축
- **독립형** (단아한화식): "PMF Survey 기획 및 운영" + "고객 인터뷰 기획 및 운영" — 테스티파이 항목과 무관한 완전 별도 구성

### 6.5 시장검증 — 알파브라더스 (4개 세부모듈 + 통합 패키지) [v0.8: 4개 모듈이 전체 법인 공통 표준 카탈로그]
알파브라더스는 자체 표준 템플릿을 보유하고 있으며, 테스티파이/블렌디드랩보다 훨씬 세분화되어 있음. 모든 모듈이 공통적으로 "BM 진단 및 고도화"로 시작해 "결과 리포트 제공"으로 마무리되는 구조. **[v0.8]** 아래 4개 모듈(FGI/사용성 테스트/기술성 테스트/시장성 테스트)은 이제 알파브라더스뿐 아니라 테스티파이/블렌디드랩/썬데이워커/ABBG도 동일하게 쓰는 시장검증 표준 카탈로그다(6.1 참고) — "통합 패키지"만 알파브라더스 고유 상품으로 남는다.

| 모듈 | 항목 구성 | 공급가액 합계(예시) |
|---|---|---|
| FGI (심층좌담회) | BM진단및고도화 / 인터뷰이 모집 / 인터뷰 스크립트 작성 / 인터뷰 진행 / 결과 리포트 제공 | 5,000,000 |
| 사용성 테스트 | BM진단및고도화 / 테스터 설계 / 일반테스터 모집 / 테스트 운영 / 결과 리포트 제공 | 5,000,000 |
| 기술성 테스트 | BM진단및고도화 / 기술성 테스트 설계 / 전문테스터 모집 / 1·2차 테스트 운영 / 결과 리포트 제공 | 5,000,000 |
| 시장성 테스트 | BM진단및고도화 / MVP 광고안 기획 / MVP 제작 / 데이터 분석 / 결과 리포트 제공 | 5,000,000 |
| **통합 패키지** (4개 모듈 번들) | BM진단및고도화(5M) / FGI(10M) / 사용성테스트(10M) / 기술성테스트(10M) / 시장성테스트(15M) | 50,000,000 |

- 각 항목마다 **표준 상품구성 설명(세부 작업 bullet 3~5개)**이 함께 정의되어 있어, 항목명뿐 아니라 설명까지 자동 삽입 가능
- 단가 산정 근거로 "SW기술자 평균임금 공표표"(PM/기획자/개발자/디자이너의 일·월 평균임금 + 제경비110% + 기술료20%)를 보유 — 자동배분 시 참고 가능한 단가 산정 기준

**전체 모듈 원본 마스터 파일 확보 (`backend/templates/alphabrothers.xlsx` — 시트: 시장성테스트/FGI/사용성테스트/기술성테스트/견적서(통합패키지), 2026-07-07 확인)**

파일 안에 4개 세부모듈 + 통합 패키지 시트가 모두 있고, 시장성테스트 시트는 실제 발급 샘플(고객사: 어니티, 견적코드 20240415-EA001, 견적일 2024.7.24, 최종제안가 5,500,000원 VAT포함)이 그대로 남아있어 항목별 단가까지 확인됨. 나머지 3개 모듈 + 통합 패키지도 동일한 파일 안에서 항목별 단가·상품구성 설명이 이미 채워진 상태로 확인됨(공급가액 합계는 4개 모듈 모두 5,000,000원으로 동일).

**[v0.9] 아래 FGI/사용성 테스트/기술성 테스트 3개 모듈의 "BM 진단 및 고도화" 행은 중복이라 카탈로그에서 삭제했다(마이그레이션 031) — 시장성 테스트 모듈에만 남는다. 원본 마스터 파일에는 여전히 이 행이 있어 값을 채우지 않으면 해당 행이 자동으로 숨겨진다(pdf_service). 아래 표는 v0.9 이전 실제 발급 이력 참고용으로 그대로 둔다.**

**FGI (심층좌담회)**

| 항목 | 상품구성 상세 | 작업일 | 수량 | 단가 | 공급가액 | 비중 |
|---|---|---|---|---|---|---|
| ~~BM 진단 및 고도화~~ [v0.9 삭제] | 1. Business Model 9 Canvas 작성 및 분석<br>2. Value Curve 작성 및 분석<br>3. 디자인씽킹 기반의 BM 분석<br>4. 진단 결과 보고 | 1 | 1 | 150,000 | 150,000 | 3.0% |
| 인터뷰이 모집 | 1. 잠재고객 기반 특성과 유사한 인터뷰이 모집(3~5인)<br>2. 고객여정지도 분석<br>3. Macro/Micro 기반의 스크립트 작성<br>4. 그룹인터뷰 실시<br>5. 인터뷰 결과 보고서 제작 | 3 | 3 | 150,000 | 1,350,000 | 27.0% |
| 인터뷰 스크립트 작성 | 1. Macro/Micro 분석 스크립트<br>2. 기능적 관점의 Pain Point 및 Gain Point 도출<br>3. 경쟁사 비교 만족도 분석, NPS 지수 조사, 4대 가치별 만족도 조사 | 5 | 3 | 180,000 | 2,700,000 | 54.0% |
| 인터뷰 진행 | 1. 인터뷰 장소 대관<br>2. 2~3시간 인터뷰 진행<br>3. 모더레이터 및 스크립터 투입 및 운영 | 3 | 1 | 200,000 | 600,000 | 12.0% |
| 결과 리포트 제공 | 1. FGI 결과 및 분석 리포트 제작<br>2. 분석 결과 보고 | 1 | 1 | 200,000 | 200,000 | 4.0% |

**사용성 테스트**

| 항목 | 상품구성 상세 | 작업일 | 수량 | 단가 | 공급가액 | 비중 |
|---|---|---|---|---|---|---|
| ~~BM 진단 및 고도화~~ [v0.9 삭제] | (FGI와 동일) | 1 | 1 | 150,000 | 150,000 | 3.0% |
| 테스터 설계 | 1. MVP TEST 플랫폼 내 모집, 체험, 데이터 수집 기간 시스템 설정<br>2. 기능별 설문지 기획<br>3. NPS, 4대 가치 만족도, 경쟁사 대비 우위·열위 영역 설문지 기획 | 3 | 3 | 150,000 | 1,350,000 | 27.0% |
| 일반테스터 모집 | 1. B2C 제품 100명 내외 / B2B 제품 30명 내외 잠재고객 테스터 모집<br>2. MVP TEST 플랫폼을 통해 참석 의향 및 테스터 승인 | 1 | 100 | 10,000 | 1,000,000 | 20.0% |
| 테스트 운영 | 1. 2주간 체험 기간 운영<br>2. 설문 참여율, 성실도 추적 관리<br>3. 리워드 보상 관리<br>4. 우수 테스터 선정 | 5 | 1 | 200,000 | 1,000,000 | 20.0% |
| 결과 리포트 제공 | 1. 사용성테스트 결과 및 분석 리포트 제작<br>2. 분석 결과 보고 | 1 | 1 | 1,500,000 | 1,500,000 | 30.0% |

**기술성 테스트**

| 항목 | 상품구성 상세 | 작업일 | 수량 | 단가 | 공급가액 | 비중 |
|---|---|---|---|---|---|---|
| ~~BM 진단 및 고도화~~ [v0.9 삭제] | (FGI와 동일) | 1 | 1 | 150,000 | 150,000 | 3.0% |
| 기술성 테스트 설계 | 1. 기능별 결함 발견 설문지 기획<br>2. 결함 수준별 카테고리화 설계 | 1 | 5 | 150,000 | 750,000 | 15.0% |
| 전문테스터 모집 | 1. ISTQB 자격증 소지자 테스터 모집<br>2. SW개발 경력 10년 이상 테스터 모집<br>3. MVP TEST 플랫폼 내 모집 운영 | 1 | 5 | 300,000 | 1,500,000 | 30.0% |
| 1·2차 테스트 운영 | 1. 오류 및 결함 발견 1차 테스트를 통한 150개 이상 도출<br>2. 개선 기간 이후 2차 결함 발견 및 진단 테스트 | 10 | 1 | 210,000 | 2,100,000 | 42.0% |
| 결과 리포트 제공 | 1. 기술성테스트 결과 및 분석 리포트 제작<br>2. 분석 결과 보고 | 1 | 1 | 500,000 | 500,000 | 10.0% |

**시장성 테스트** (실제 발급 샘플 — 고객사: 어니티, 견적코드 20240415-EA001, 견적일 2024.7.24, 최종제안가 5,500,000원 VAT포함)

| 항목 | 상품구성 상세 | 작업일 | 수량 | 단가 | 공급가액 | 비중 |
|---|---|---|---|---|---|---|
| BM 진단 및 고도화 | 1. Business Model 9 Canvas 작성 및 분석<br>2. Value Curve 작성 및 분석<br>3. 디자인씽킹 기반의 BM 분석<br>4. 진단 결과 보고 | 1 | 1 | 150,000 | 150,000 | 3.0% |
| MVP 광고안 기획 | 1. P-S-S-T 기반 Value Proposition MVP 기획<br>2. 15,000~20,000px, 3~4개 분량의 상세페이지 기획<br>3. MVP A/B 테스트 기반 3~4개 분량 썸네일 기획 | 3 | 3 | 450,000 | 1,350,000 | 27.0% |
| MVP 제작 | 1. 3개 분량의 MVP 상세페이지 디자인 작업<br>2. 3개 분량의 MVP 썸네일 디자인 작업<br>3. MVP별 랜딩페이지 구축<br>4. GA 등 데이터 분석 Tool 구축 | 10 | 3 | 800,000 | 2,400,000 | 48.0% |
| 데이터 분석 | 1. 페이스북, 인스타그램 광고 데이터 분석<br>2. 고관여자 설문 데이터 분석<br>3. 정성데이터(주관식 설문) 자연어 처리<br>4. 정량데이터 시각화 처리<br>5. 경쟁사 시장성데이터 비교 분석 | 5 | 1 | 900,000 | 900,000 | 18.0% |
| 결과 리포트 제공 | 1. 시장성 데이터 분석 리포트 제작<br>2. 분석 결과 보고 | 1 | 1 | 200,000 | 200,000 | 4.0% |

**통합 패키지** (4개 모듈 번들, 공급가액 합계 50,000,000원 — "견적서" 시트)

| 항목 | 상품구성 상세 | 작업일 | 수량 | 단가 | 공급가액 | 비중 |
|---|---|---|---|---|---|---|
| BM 진단 및 고도화 | 1. Business Model 9 Canvas 작성 및 분석<br>2. Value Curve 작성 및 분석<br>3. 디자인씽킹 기반의 BM 분석<br>4. 진단 결과 보고 | 5 | 5 | 1,000,000 | 5,000,000 | 10.0% |
| FGI (심층 그룹 인터뷰) | 1. 잠재고객 기반의 인터뷰이 6인 모집<br>2. 고객여정지도 분석<br>3. Macro/Micro 기반의 스크립트 작성<br>4. 그룹인터뷰 실시<br>5. 인터뷰 결과 보고서 제작 | 30 | 2 | 5,000,000 | 10,000,000 | 20.0% |
| 사용성 테스트 | 1. 잠재고객 기반의 사용성 테스터 100인 모집<br>2. 고객여정지도 분석 기반의 테스트 설계<br>3. 경쟁사 비교 만족도 분석, NPS 지수 조사, 4대 가치별 만족도 조사<br>4. 테스트 결과 보고 | 30 | 2 | 5,000,000 | 10,000,000 | 20.0% |
| 기술성 테스트 | 1. ISTQB 자격증 소지자 및 개발 경력 10년 이상 테스터 5인 모집<br>2. 오류 및 결함 발견 1차 테스트를 통한 150개 이상 도출<br>3. 개선 기간 이후 2차 결함 발견 및 진단 테스트<br>4. 진단 결과 보고 | 30 | 2 | 5,000,000 | 10,000,000 | 20.0% |
| 시장성 테스트 | 1. 진단 결과를 기반으로 MVP 가설 수립<br>2. MVP 광고안 제작<br>3. 메타 플랫폼 기반의 시장성 테스트<br>4. 시장성데이터 분석 및 결과보고 | 30 | 3 | 5,000,000 | 15,000,000 | 30.0% |

→ 통합 패키지 시트는 공급가액 수식이 `=수량×단가`로, 다른 모듈 시트(`=작업일×수량×단가`)와 계산 방식이 다름 (작업일이 수식에 반영되지 않음). 원본 그대로 옮긴 것이며 임의로 통일하지 않았음.

### 6.6 나머지 — 썬데이워커 / ABBG

**썬데이워커** — **[정정, 팀장 확인 완료]** 시장검증·마케팅 과업도 취급함. 이 프로젝트에서 썬데이워커 명의로 견적서를 발급할 때 **항목 내용은 6.1(시장검증 — v0.8부터 전체 법인 공통 표준 카탈로그)/6.2(마케팅) 카탈로그를 그대로 사용**하고, **표 구조(양식)만 아래 예시의 컬럼 레이아웃을 재사용**

아래는 양식 참고용으로만 남겨둔 예시 (경기도경제과학진흥원 대상, 행사운영 건 — 이 항목 내용 자체는 이 프로젝트에서 쓰지 않음):
| 항목 | 공급가액 |
|---|---|
| 대관료 | 20,000,000 |
| 식비 | 4,500,000 |
| 상담회 심사 | 9,000,000 |
| 아나운서 섭외 | 700,000 |
| 네트워킹 행사 | 1,500,000 |
| 운영 부대비용(현수막, X배너) | 4,000,000 |
| 인건비(프로젝트 총괄) — FTE 40% | 1,449,034 |
| 인건비(프로젝트 기획) — FTE 40% | 4,347,102 |
| 인건비(행사운영) — FTE 30% | 4,999,950 |
| (DC 할인 반영) | -545,695 |

→ 컬럼 구조(품명/투입MM/수량/단가/공급가액/세액, 항목별 세액 계산, DC 조정 필드)는 재사용하되, 채워지는 항목명·비중은 6.1/6.2 카탈로그에서 가져옴
→ ⏳ **[보류 — 실제 발급 샘플 확보 후 결정]** 시장검증/마케팅 항목을 이 컬럼 구조에 넣을 때 "인건비 FTE% 계산 로직"이나 "투입MM" 컬럼을 그대로 쓸지, 다른 법인처럼 단순 단가×수량으로 처리할지는 지금 확정하지 않음 (8장 질문 6 참고)

**ABBG** — **[v0.4 정정]** 확보된 xlsx에 있던 "UXUI/디자인 제작"(버튼·레이어팝업·로고 등)은 ABBG의 실제 취급 업무가 아니라 원본 파일의 예시 내용이었음이 확인됨 (2026-07-07). ABBG도 다른 4개 법인과 동일하게 마케팅/광고대행/고객검증/시장검증을 취급하며, **표 구조(품명/상세내용/소요일/작업수량/단가/공급가액)만 재사용**하고 항목 내용은 다른 법인 카탈로그를 차용해 채운다 (부록 B 참고).

⚠️ ABBG는 4개 과업종류 전부 실제 카탈로그·단가 데이터가 없는 상태 — 자동배분 시 전량 타 법인 카탈로그 임시 차용 필요 (Phase 4에서 처리, 8장 질문 7 참고)

---

## 7. 항목 자동생성 로직 + LLM 프롬프트 설계

### 7.1 처리 흐름
1. 입력 수집: 총액, VAT포함여부, 과업종류, 법인
2. `ItemCatalog`에서 (법인, 과업종류) 조합 조회 → **[v0.4 변경]** 실제 카탈로그가 없어도 더 이상 경고하지 않음. 대신 해당 과업종류의 대표 카탈로그(예: 마케팅=테스티파이, 고객검증=블렌디드랩)를 임시로 빌려와 사용 — 어떤 법인이든 과업종류 모두 발급 가능해야 하므로 (1.4 참고). **[v0.8]** 시장검증은 5개 법인 모두 알파브라더스 표준 카탈로그의 자체 행을 갖고 있어(6.1) 사실상 이 fallback을 탈 일이 없다 — 기준 법인만 알파브라더스로 맞춰 둠
3. 카탈로그가 여러 모듈로 구성된 경우, PM에게 모듈 선택 요청 (필수 모듈은 자동 포함, 옵션 모듈은 선택)
4. 선택된 모듈의 항목들을 하나의 리스트로 병합
5. LLM 호출 — 과거 비중(%) 데이터를 참고하여 총액을 항목별로 배분 (아래 7.2 프롬프트)
6. 반올림 처리: 항목별 금액을 만원 단위 등으로 정리, 차액은 마지막 항목 혹은 별도 "절삭/DC" 필드에서 흡수
7. VAT 계산: 총액이 VAT포함이면 공급가액=총액/1.1(반올림), VAT=총액-공급가액. VAT별도면 공급가액=총액, VAT=총액×0.1
8. `EntityQuote` 레코드 생성 → PDF 렌더링

### 7.2 LLM 프롬프트 템플릿 (예시 — 시장검증/테스티파이)

```
[System]
당신은 견적서 항목별 금액 배분 담당자입니다. 아래 표준 항목 카탈로그와 과거 비중 데이터를 참고하여,
주어진 총액을 각 항목에 배분하세요. 반드시 JSON으로만 응답하고, 배분된 금액의 합은 입력된 공급가액과
정확히 일치해야 합니다.

[표준 항목 카탈로그 — 법인: 테스티파이 / 과업종류: 시장검증]
모듈 A. 시장성 테스트 (필수)
- BM 진단 및 고도화 (과거 비중 5.6%)
- MVP 광고안 기획 및 제작 (과거 비중 44.4%)
- 정량 데이터 시계열 분석 (과거 비중 22.2%)
- 정량 데이터 클러스터 분석 (과거 비중 22.2%)
- 결과 리포트 제공 (과거 비중 5.6%)

모듈 B. 설문형 시장검증 (옵션)
- 설문 설계 (과거 비중 5.6%)
- 설문응답자 모집 및 운영 *리워드 포함 (과거 비중 44.4%)
- 설문 데이터 가공 및 시각화 (과거 비중 38.9%)
- 결과 리포트 제공 (과거 비중 11.1%)

[입력]
공급가액(VAT 제외): 18,000,000원
선택된 모듈: [모듈 A, 모듈 B]

[출력 형식]
{
  "items": [
    {"category": "시장성 테스트", "name": "BM 진단 및 고도화", "amount": 500000},
    ...
  ],
  "subtotal": 18000000
}
```

- 과업종류·법인이 달라지면 [표준 항목 카탈로그] 블록만 6장의 해당 섹션으로 교체하면 동일 프롬프트 구조 재사용 가능. **[v0.8]** 위 예시는 6.1의 v0.8 이전 테스티파이 카탈로그를 그대로 쓴 것 — 시장검증은 이제 법인과 무관하게 6.1/6.5의 4개 모듈(FGI/사용성 테스트/기술성 테스트/시장성 테스트) 표준 카탈로그로 교체해서 써야 함
- 옵션 모듈이 없는 과업종류(예: 광고대행-블렌디드랩, 단일항목형)는 모듈 선택 단계를 생략하고 항목 1개에 총액 그대로 배정

### 7.3 채팅 수정 시 프롬프트 확장
기존 항목 리스트 + 수정 요청 원문을 함께 LLM에 전달하여 diff를 JSON으로 받는 방식으로 확장. 이때 **수정 범위(이번 건만 / 카탈로그 갱신)를 먼저 판별**한 뒤 각각에 맞는 대상 데이터(EntityQuote 또는 ItemCatalog)에 적용:

```
[System]
아래는 기존 견적 항목이며, 사용자의 수정 요청을 반영해 재배분하세요.
제약조건(예: 총액 유지)이 있으면 반드시 지켜야 합니다.
먼저 이 요청이 "이번 견적 건만" 수정하려는 것인지, "앞으로도 계속 적용될 기본값(카탈로그)"을
바꾸려는 것인지 판별하고 scope 필드로 명시하세요. 판별이 애매하면 scope를 "ambiguous"로 표시하세요.

[기존 항목]
(현재 EntityQuote의 LineItems 그대로 삽입)

[수정 요청]
"3번 항목 금액을 200만원 낮추고, 총액은 그대로 유지해줘"

[출력 형식]
{
  "scope": "quote_only" | "catalog_update" | "ambiguous",
  "items": [...],
  "changed_items": [...]
}
```

- `scope`가 `ambiguous`면 시스템이 "이번 견적에만 반영할까요, 카탈로그 기본값도 바꿀까요?"로 1회 확인
- `scope`가 `catalog_update`면 EntityQuote뿐 아니라 ItemCatalog도 함께 갱신(버전 이력 남김)

---

### 7.4 썬데이워커 전용 파생 컬럼 (투입 MM / 세액) — 2026-08-11 추가

썬데이워커 원본 양식에는 항목마다 "투입 MM"·"세액" 칸이 있지만, 카탈로그 데이터 모델에는 역할별
투입 인원·MM 개념이 없다(`item_catalogs`는 `work_days`/`quantity`만 보유). 정확한 값을 낼 방법이
없어 우선 아래처럼 간단한 파생값으로 채우고, **사용자가 채팅/직접편집으로 언제든 고치는 것을
전제로 한다**(향후 카탈로그에 역할·MM 컬럼을 추가하는 정식 데이터 모델은 별도 논의 필요 — 8장에
미해결 질문으로 등록).

- **세액**: `공급가액 × 0.1`을 반올림해 그대로 사용(수식이 아니라 우리가 계산해 값으로 채움). 원본
  양식의 세액 칸은 항목별 표시용일 뿐 합계 수식(`=공급가액합계×0.1`)과는 무관해 총액 계산에 영향
  없음을 확인함.
- **투입 MM**: `work_days × quantity ÷ 20`(영업일 20일=1MM 근사) 값을 소수 둘째 자리까지 반올림.
  실제 역할별 투입 인원 배분과는 무관한 근사치이며, 정확도를 요구하는 값이 아니다.
- 두 값 모두 `pdf_service._derive_extra_columns()`에서 계산하고, 실제 PDF 발급과 웹 미리보기가
  동일한 값을 쓰도록 그 한 함수를 공유한다.
- "투입 리소스"(역할별 인원 목록, 원본 양식 20~22행)는 이 파생 로직 대상이 아니다 — 근거로 삼을
  최소한의 데이터조차 없어 여전히 항상 비워둔다(`role: "labor_fte"` 블록, 4.6 참고).

---

## 8. 미해결 질문 (영민 팀장 컨펌 필요)

1. 마크업 "10~15%" 룰이 언제부터 적용된 정책인지 — 과거 히스토리(5.6%~36.6%)는 그 이전 사례라 예외인지
2. 항목 구성 모드(미러링형/독립형) 기본값을 뭘로 할지, 매번 선택하게 할지
3. 채팅/직접편집 수정 시 본견적서만 수정하는지, 비교견적서도 연동해서 같이 조정되는지
4. 비교견적서의 과업종류 자체를 본견적서와 다르게 설정하는 것이 일반적인 관행인지, 예외적 케이스인지
5. 광고대행(블렌디드랩)이 항상 단일항목형인지, 세분화된 버전도 있는지
6. **[해결, v0.4]** 5개 법인 모두 마케팅/광고대행/고객검증/시장검증을 공통 취급하는 것으로 확인됨 (1.4 참고) — 더 이상 "비교견적 상대가 없는 법인" 문제는 없음. 다만 실제 카탈로그 데이터가 없는 (법인×과업종류) 조합의 항목을 어떤 법인 카탈로그로 채울지는 Phase 4에서 설계. ⏳ **[보류]** 썬데이워커 양식의 "인건비 FTE%/투입MM" 컬럼을 실제 시장검증·마케팅 항목에도 그대로 적용할지는 실제 발급 샘플 확보 후 결정
7. ABBG의 실제 항목별 단가 데이터를 추가 확보할 수 있는지 (현재 빈 템플릿뿐)
8. 알파브라더스의 "SW기술자 평균임금 공표표" 기반 단가 산정을 다른 법인 견적에도 참고 기준으로 쓸 것인지, 알파브라더스 전용 로직으로 둘 것인지
9. 부가세 절삭·반올림, 썬데이워커식 "DC" 조정의 표준 규칙 존재 여부
10. 카탈로그(마스터 템플릿) 수정 권한 — 모든 PM이 가능한지, 특정 담당자만 가능한지. 카탈로그 변경 시 승인 절차가 필요한지
11. **[v0.5 신규]** 비교견적서 항목명을 본견적서와 다르게 구성할 때, 구체적으로 어떤 단어 매핑 규칙을 쓸지 (예: 법인별 동의어 사전을 둘지, LLM이 매 건 자유 변형할지)
12. **[v0.5~v0.6, 해결됨]** 카카오 톡스토어/네이버 쇼핑 광고 대행은 실 샘플 확보(전자) 및 사용자 지시(후자)로 6.2에 반영 완료. "광고형 마케팅 대행 용역"은 애초 마케팅이 아니라 광고대행 소속이라 잘못 정리했었는데, 사용자 정정(2026-08-08)으로 **광고대행 자체가 마케팅에 흡수**됨(1.4 v0.6) — 이제 위저드가 이미 노출 중인 "마케팅" 선택 안에서 대안 상품으로 바로 선택 가능해져 별도 노출 작업이 필요 없어짐. **고객검증**은 여전히 위저드 미노출 상태로 남아있어 언제 노출할지는 계속 미해결
13. **[v0.8, 해결됨]** 시장검증 항목 정의를 사용자가 직접 정리한 대분류/중분류/소분류 체계로 재정의해 달라는 요청 — 확인 결과 그 체계(4개 중분류 + 개조식 세부항목)는 이미 알파브라더스 카탈로그(6.5)와 문구까지 정확히 일치했다. 사용자가 "5개 법인 공통 표준 카탈로그로 통합"을 선택해(2026-08-09) 테스티파이/블렌디드랩/썬데이워커의 실제 발급 이력 기반 카탈로그를 대체했다 — 상세는 6.1/v0.8 변경 요약 참고. 마케팅에도 동일하게 개조식 세부항목을 넣어달라는 요청은 아직 미해결 — 실제 발급 샘플 등 근거 문구가 없어 사용자에게 출처를 확인 중 (임의로 지어내지 않는다는 프로젝트 원칙 때문)
14. **[v0.9 신규, 미해결]** 실무자 정보: "썬데이워커·ABBG는 시장검증 발행 이력이 없고, 실제로는 시장검증+마케팅 비교견적=블렌디드랩 / 디자인=ABBG / 컨설팅·AI·개발=ABBG·썬데이워커"라는 내용이 1.4(2026-07-07 팀장 확인 — "5개 법인 모두 마케팅/고객검증/시장검증 공통 취급")와 정면으로 상충함. 근거를 확인하던 중 `data/(알파) 이루리랩스_UXUI디자인 견적서_250715_001.pdf`에서 **알파브라더스가 실제로 "UXUI 디자인" 업무를 발급한 이력 1건**을 확인했다(2025.07.15, 이루리랩스, 공급가액 250만원 — 단가 산정 근거인 "SW기술자 평균임금 공표표"가 6.5에 이미 언급된 표와 정확히 일치해 신뢰도 높음). 다만 표본이 1건뿐이고 "컨설팅/AI/개발" 쪽 근거는 아직 없어(`data/(알파) 마케팅, 개발, 디자인, 특허 비교견적서 모음_250917.xlsx`는 8개 시트 전부 스프린트·FLES·안르 등 **타사 명의** 견적서라 알파브라더스 자체 카탈로그로 쓰기 어려움), **신규 과업종류(디자인 등) 도입은 2026-08-10 사용자 결정으로 보류**. 법인별 취급 과업종류 재정의는 팀장 재확인 또는 추가 발급 이력 확보 후 재논의.
15. **[v0.10 신규, 미해결]** 썬데이워커 "투입 리소스"(역할별 인원, 원본 양식 20~22행 `role: labor_fte` 블록)를 실제로 채우려면 카탈로그에 역할(PM/디자이너/개발자 등)별 MM 데이터가 있어야 하는데 현재 데이터 모델에 전혀 없음(7.4 참고). 현재는 "투입 MM"만 작업일×수량 기반 근사치로 채우고, "투입 리소스"는 계속 비워 사용자가 직접 채우는 것으로 2026-08-11 임시 결정. 정식 데이터 모델(카탈로그에 역할·MM 컬럼 추가)을 도입할지는 팀장 확인 필요.

---

## 9. 다음 단계 제안

1. 위 미해결 질문 팀장 컨펌 (특히 6, 7번 — 썬데이워커/ABBG 데이터 공백)
2. ⏳ **[보류 항목]** 썬데이워커 시장검증·마케팅 견적 실제 발급 샘플 확보 시, 인건비 FTE%/투입MM 컬럼 적용 여부 결정 후 6.6·데이터모델 반영 (지금은 임시로 다른 법인과 동일한 단가×수량 방식으로 개발 진행, 샘플 확보 후 조정)
3. 화면 흐름(와이어프레임) 설계 — **10장 UI/UX 벤치마킹 참고안** 활용
4. LLM 프롬프트 실제 테스트 (7.2 템플릿으로 파일럿)
5. 기술 스택 확정 및 개발 착수

---

## 10. UI/UX 벤치마킹 참고안 (추후 구현 — 지금 당장 작업 대상 아님)

사용자가 참고 서비스("AutoQuote SaaS Edition v2.0" 스타일, 2026-07-07 스크린샷 공유)를 벤치마킹해 화면 설계에 반영하고 싶다고 요청. **Phase 3~5(핵심 기능 구현)를 먼저 끝낸 뒤, 화면 흐름/와이어프레임 설계 단계(9장 3번)에서 아래 구조를 기준으로 실제 적용한다.**

**[2026-07-08 재확인]** 견적서 목록 화면(10.2)과 설정/브랜딩 화면(10.3) 스크린샷을 다시 공유받아 아래 구조가 정확히 일치함을 재확인함. 지금 진행 중인 위저드(Phase 4 채팅/직접수정 포함)를 먼저 완성하고, 이 10장 구조는 계속 "추후 구현"으로 보류.

### 10.1 정보구조 (좌측 사이드바 내비게이션)
- **견적서 목록**: 발행한 견적서를 표로 관리·조회 (기본 화면)
- **견적서 작성**: 새 견적서 생성 플로우 (Phase 3에서 이미 구현한 위저드가 이 자리에 들어감)
- **설정**: 법인별 원본 파일·브랜딩 관리
- **사용자 관리**: 현재 스코프 아님 — 인증 미도입 상태(CLAUDE.md 2장)라 다인 사용이 필요해지는 시점에 재검토

### 10.2 견적서 목록 화면
- 표 컬럼: 견적번호 / 고객사(수신자) / 발행일 / 총 합계
- 행별 액션 아이콘: 복사(재발급용 복제) / 수정 / 보기 / 인쇄(PDF) / 삭제
- 우측 상단 "+ 새 견적서 작성" 버튼 → 견적서 작성 화면으로 이동
- → `EstimateSet`/`EntityQuote`/`QuoteVersion` 데이터를 그대로 이 표에 매핑하면 됨 (5장 데이터모델과 궁합 좋음)

### 10.3 설정 화면 — **참고 서비스와 다르게 적용해야 하는 부분**
참고 서비스는 "테마 선택"(실리콘밸리/야피/공공기관/큐트 등 프리뷰)과 "직인 자동생성"(도장 이미지 없으면 CSS로 생성) 기능이 있었으나, **우리 프로젝트에는 그대로 적용하지 않는다**:
- 우리는 5개 법인 모두 이미 실제 원본 파일(로고·도장·컬럼구조 내장)을 보유하고 있어(4.2, `QuoteTemplate`), 테마를 새로 고르거나 도장을 자동 생성할 필요가 없음 — 오히려 원본을 임의로 바꾸면 안 됨(4.2 "고정정보는 절대 건드리지 않음")
- 대신 설정 화면은 **"5개 법인 각각의 원본 파일 확인·교체 업로드" 관리 화면**으로 변형 적용 (Phase 2 `quote_templates.storage_path` 갱신 기능의 UI화)
- "비즈니스 프로필" 탭 같은 개념은 필요 없음 (법인 고정정보를 DB에 중복 저장하지 않기로 한 결정과 충돌 — 001 참고)

### 10.4 [v0.5 신규, 추후 개발] 프로젝트 단위 견적 생성 (스케일업팀용)

**지금 이 프로젝트의 개발 대상이 아님.** 현재 위저드는 "과업 단위"(데이터솔루션팀 사용 — 시장검증/마케팅 등 카탈로그 기반 자동배분)만 다룬다. 사용자가 향후 별도로 확장하고 싶다고 밝힌 방향을 기록만 해둔다:

- 사용 주체: 스케일업팀 (데이터솔루션팀과 다른 팀)
- 입력 방식: RFP(과업지시서) PDF 업로드 → AI가 PDF에서 과업 세부내용을 자동 인식해 견적 항목(재료비/인건비 등) 구성
- 항목 구조: 프로그램별로 취급 과업 항목이 미리 정해져 있음 (현재 6장 카탈로그 구조와는 별개 체계)
- 경비 규칙: 일반관리비 / 이윤(상한 4%) / 소계 / 부가가치세 / 합계 등 정해진 계산 규칙 적용 필요
- 착수 조건: 실제 RFP 샘플 확보 및 경비 규칙 확정 후 별도 PRD 섹션으로 설계 — 그 전까지는 백로그로만 유지

---

## 부록 A. 히스토리 분석 방법 (도메인 지식 불필요)

마케팅/시장검증 내용을 이해할 필요 없이, 아래 4가지 구조적 변화만 체크:

| 체크 항목 | 확인 방법 |
|---|---|
| 총액 변경 여부 | 안 바뀌었으면 → 항목 재배분만 발생한 것 |
| 항목 개수 변경 | 늘었으면 분리, 줄었으면 통합 |
| 항목명 변경 여부 | 금액 동일 + 이름만 다르면 → 표현 수정 |
| 단가/작업일 변경 여부 | 실제 협상/네고 결과 |

파일 비교 시 그룹핑 기준: `[법인]_[고객사]_[문서유형]_[날짜]` 패턴으로 정렬 후, 동일 그룹 내 날짜순으로 버전 간 위 4가지를 비교.

## 부록 B. 법인별 실제 카탈로그 확보 현황 (v0.8 — 5개 법인 모두 3개 과업종류 공통 취급, 광고대행은 마케팅에 흡수, 시장검증은 전체 법인 공통 표준 카탈로그로 통합)

**[v0.8]** 시장검증은 더 이상 법인별 확보 여부를 따지지 않는다 — 5개 법인 전부 알파브라더스 표준 카탈로그(6.1/6.5, 4개 모듈)의 자체 행을 갖고 있다. 아래 표는 마케팅/고객검증만 대상으로 한다.

| 법인 | 실제 카탈로그 확보 과업종류 | 카탈로그 미확보 과업종류 (Phase 4에서 타 법인 카탈로그 임시 차용 필요) |
|---|---|---|
| 테스티파이 | 마케팅(대안상품 4종) | 고객검증 |
| 알파브라더스 | — | 마케팅(테스티파이 카탈로그 폴백 차용), 고객검증 |
| 블렌디드랩 | 마케팅(대안상품 2종), 고객검증 (충분) | — |
| 썬데이워커 | 마케팅 (테스티파이 대안상품 4종 그대로 미러링 — 6.6 참고) | 고객검증 |
| ABBG | — (양식만 보유, 실단가 데이터 없음) | 마케팅(테스티파이 폴백 차용), 고객검증 |
